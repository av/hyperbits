---
title: Getting Started
description: Add hyperbits animation compositions to a HyperFrames project
---

import { Tabs, TabItem, Aside } from '@astrojs/starlight/components';
import { HeroShowcase } from '@showcases/HeroShowcase';

hyperbits is a catalog of self-contained HTML + GSAP animation bits for [HyperFrames](https://github.com/heygen-com/hyperframes). Each bit is a composition file with its own CSS, paused timeline, and `data-composition-variables`.

## Installation

<Tabs>
  <TabItem label="CLI">
    Search and add bits from a HyperFrames project:

    ```bash
    npx hyperbits find "fade in text"
    npx hyperbits add fade-in
    ```

    `add` writes `compositions/fade-in.html` and prints a `data-composition-src` snippet.
  </TabItem>
  <TabItem label="hyperframes add">
    Point the project registry at the hyperbits catalog, then use the HyperFrames CLI:

    ```json title="hyperframes.json"
    {
      "registry": "https://hyperbits.pages.dev"
    }
    ```

    ```bash
    npx hyperframes add fade-in
    ```
  </TabItem>
  <TabItem label="npm">
    ```bash
    npm install hyperbits
    ```
  </TabItem>
  <TabItem label="MCP">
    ```json title="mcp.json"
    {
      "mcpServers": {
        "hyperbits": {
          "command": "npx",
          "args": ["-y", "hyperbits", "mcp"]
        }
      }
    }
    ```
  </TabItem>
</Tabs>

## Prerequisites

Bits load GSAP themselves from:

```text
https://cdn.jsdelivr.net/npm/gsap@3.14.2/dist/gsap.min.js
```

Helpers ship as `hyperbits.iife.js` (CDN or the local `dist/` build). HyperFrames does not inject either.

<Aside>
Need a HyperFrames project first? Run `npx hyperframes init`.
</Aside>

## Embed a bit

After `hyperbits add fade-in`:

```html
<script src="https://cdn.jsdelivr.net/npm/gsap@3.14.2/dist/gsap.min.js"></script>
<script src="https://unpkg.com/hyperbits/dist/hyperbits.iife.js"></script>

<div data-composition-src="compositions/fade-in.html"></div>
```

Override knobs declared in `data-composition-variables` with `data-variable-values` on the host, or `--variables` on `hyperframes render`.

## Helpers

The same helpers bits use are available as ES modules:

```js
import { interpolate, stagger, viewport } from 'hyperbits/helpers';
```

or on the IIFE global `hyperbits` inside a composition `<script>`.

<HeroShowcase client:only="react" bitNames={["fade-in", "word-by-word", "char-by-char"]} layout="row" />

## Next steps

1. Browse the [catalog](/docs/bits-catalog)
2. Read the [CLI](/docs/cli) and [MCP](/docs/mcp) pages
3. Use a helper from the [reference](/docs/reference/interpolate)
