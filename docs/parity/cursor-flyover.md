# cursor-flyover

Category: scenes-3d. Fidelity 1 -> 4 (1-5, judged against the remotion-bits original rendered side by side).

Was: a mock dashboard with widgets and a cursor tweened between them.
Now: the 120vmin gallery image, camera stops at the four corners at 0.2 widths, 45 frame steps, and a StepResponsive-style cursor tracking the corners with 35 frame easeInOutCubic moves and the 1-2-1 scale pulses.
Not fully reproducible: the original loads /gallery.png from the docs site; the port embeds a JPEG copy, and the reference render shows an empty frame.
