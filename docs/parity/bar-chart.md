# bar-chart

Category: text-animations. Fidelity 2 -> 5 (1-5, judged against the remotion-bits original rendered side by side).

Was: bars grew with power2.out and a 0.27s stagger; labels, heading offset and counters were laid out differently and GSAP silently ignored the Remotion ease names.
Now: the original layout (14vw labels, 60vw bars, 7vmin rows, heading offset by the label column), delays of 15 + 8i frames, easeOutQuart bars and counters over 45 frames, labels sliding 3vmin, counters fading over 15 frames, primary gradient bars.
