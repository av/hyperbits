# list-reveal

Category: staggered-motion. Fidelity 3 -> 4 (1-5, judged against the remotion-bits original rendered side by side).

Was: 72% wide rows, 10vmin tall, no outer glide.
Now: 600x80 rows with 16px gaps on the 1080x1080 canvas, the list gliding from +200 to -100px over 30 frames while rows pop in over 40 frames with a 3 frame stagger. Emoji icons are replaced by inline SVG (repo rule), so the icon column differs.
