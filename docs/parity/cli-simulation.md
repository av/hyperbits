# cli-simulation

Category: text-animations. Fidelity 2 -> 5 (1-5, judged against the remotion-bits original rendered side by side).

Was: output lines faded in as blocks; regular weight; prompt colours differed.
Now: the command types at 3 frames per character with a cursor that hides after completion; the four output lines type at 2 characters per frame from frame 90 with 20 frame pauses; bold 3vmin JetBrains Mono on #1e1e1e with the original prompt colours.
